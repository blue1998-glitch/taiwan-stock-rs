name: Daily Ranking
on:
  schedule:
    - cron: '30 6 * * 1-5'
  workflow_dispatch:

jobs:
  run-ranker:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install yfinance pandas requests

      - name: Build Themes Database
        run: python data/build_themes.py

      - name: Run ranking script
        run: python rank_all.py

      - name: Commit and push rankings
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/theme_mapping.json market_rankings.json || true
          git commit -m "Update full market rankings and theme database [skip ci]" || exit 0
          git pull origin main --rebase
          git push origin main
